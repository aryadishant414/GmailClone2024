

const Emails = ({openDrawer}) => {
    return (
        <div style={ openDrawer ? { marginLeft: 250, width: '100%' } : {width: '100%'}}>
            Hello From Emails
        </div>
    )
};

export default Emails;